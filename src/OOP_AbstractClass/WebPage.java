package OOP_AbstractClass;

public abstract class WebPage {
    //To check that "abstract class" supporting "multiple" - We have created this class
    //Now, IN "LoginPage", try extending 2 classes (Page,WebPage)
    //If Java does not give any error then multiple inheritance is supported. Otherwise - NO
    //Abstract classes are not supporting "multiple" inheritance
}
